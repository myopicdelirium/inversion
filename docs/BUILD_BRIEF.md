# Build brief

Read `CLAUDE.md` first. It is binding. This brief is orientation, not law.

## The mechanism in one paragraph

Agents have drives. Each drive has an urgency (computed instantly from body state and percepts) and a weight (the agent's current valuation of that drive). Weights chase urgencies through a first-order lag with a per-drive time constant. Action selection is myopic argmax over action values, which are weight-weighted expected urgency reductions. That is the entire kernel. Preference inversion, when it eventually appears, is a timing failure: a commitment drive has accumulated weight over a long horizon, a lethal threat arrives, survival urgency spikes instantly, but the survival weight cannot relax back to dominance before the decision is forced. The agent dies mid-reversion. Death is a failure to re-prioritize in time, never a valuation of death.

## Why the ordering of phases matters

The inertia law ships in Phase 1, with base drives only, before any commitment machinery exists. The lag is measured and validated as an artifact then. When sacrifice appears in later phases, the mechanism provably predates the phenomenon: nobody can claim it was added to produce the result, because it was there when agents could only eat, rest, and flee.

## Phase plan (provisional; each phase gets its own spec before code)

1. **Homeostat kernel.** Base drives (energy, safety, rest), uniform lagged update law, myopic argmax, deterministic harness, manifest writer. Validation artifact: empirically fitted lag time constant matches the declared parameter.
2. **Commitment as an ordinary drive.** The same drive class with slow accumulation dynamics (attachment to a target: place, agent, activity). No new mechanism. Validation: commitment accumulates and decays on its declared timescales; no behavioural change in environments without commitment targets (golden runs from Phase 1 still pass).
3. **Collision environments.** Environments where threat onset speed is controllable, so the timing collision becomes reachable. Validation: the null test: telegraphed threats produce collapsing sacrifice rates at fixed commitment; sudden threats do not.
4. **Sweep machinery.** Batch runner over (accumulation rate, reversion time constant, threat onset speed, ...); manifests per run; results aggregation.
5. **The phase diagram.** Population-scale sweeps (10k+ agents, thousands of runs). The map of where inversion lives and where it does not is the product.

## Current status

Phase 5 complete: THE DIAGRAM EXISTS. 738 runs across three diagrams plus a deterministic 10,000-agent record (3000 ticks in 39s). Inversion lives above a commitment-depth threshold (bond_init between 0.2 and 0.4) and saturates under gradual arrival of harm (ramp 200+); warning ramps 800+ are a null region at every depth. The psychology plane says survival of sudden threats belongs to quick fear (tau_safety dominates, 0.14 to 0.78), not shallow attachment. Composition: depth selection is universal; clock selection appears only in the gradual-arrival regime and deepens as the cook slows. Phase 6 complete: bonds to agents. Cohesion emerges (pairs at distance 1.2 vs 31 unbonded); grief is the phase 2 formula at infinity, exact on every bereaved transition, fading on the bond decay clock; and the declared pull-into-the-storm cannot exist because partners are moving targets: person-attachment measured about twelvefold protective vs place-attachment under the same storm. Phase 7 complete: bounded attention (Amendment 3), one drive-agnostic law: heard urgency = raw urgency times (weight over loudest weight) to the sharpness power. The origin case exists: bereaved agents starve in mourning (87 of 618 at kappa 2, zero at kappa 0, zero baseline), hunger breaking through only at the brink. The symmetry holds (hunger-dominance dies in hazards at 15x). Discovery: sharpness has a viability boundary (everyday survival collapses 0.99 to 0.11 across kappa 0 to 1) yet moderate sharpness is protective in acute crises: crisis minds vs maintenance minds on one axis. Phase 8 complete: the mire (storm_snare, a grip axis). The rescue trap exists: agents whose living partner is held inside die at 2.4x the rate of those whose partner is safe, 88% of them by entering after the trapping; unbonded controls show zero pull across 127 split pairs. Deep attachment partly forecloses the trap by cohesion (pairs are never apart); its prime demographic is intermediate attachment. Phase 9 complete: the human axes mapped, and an artifact caught in-session. On the place map, sharpness 0.25-1.0 is annihilating (mortality 1.0 vs 0.255 transparent) via the zero-trap: a drive with weight exactly 0 is unhearable forever, so fear spawned at zero is unacquirable. Extreme sharpness plus deep place-bonds is peacetime collapse: 199-200 of 200 starve at home before any threat (void cells marked; regime goldened honestly). The tar pit confirmed exactly as pre-registered (0 to 0.68, control flat). Phase 10 complete: the whisper floor (Amendment 3 addendum). Fear becomes acquirable, the peacetime collapse is cured, the kappa axis is smooth. The floor-boundary specificity claim was WITHDRAWN under replication (baseline starvation is run-bistable); what survives both seed sets: the floor-0 origin case, persistent bereaved neglect under any measured floor (8.5-14.6%), and hypervigilance starvation as real but bistable. Phase 11 complete: FARSIGHT (Amendment 4). Action values integrate predicted world consequences over a declared horizon under frozen current weights. The answer to the program's deepest question, replicated: inversion was myopia all the way down at every coordinate measured. Foresight defeats the boiling frog (1.000 to 0.114, fresh seeds 0.018) and abolishes the rescue trap (pull falls into the control noise band from a myopic 2.4x), with rescues aborted en route as fear catches up. Foresight costs the everyday world 7 points (Q4 failed as declared). The knowing-and-going cell (foresight plus attention) also shows no pull. Phase 12 complete: the search, replicated over 864 runs. The clear-eyed REGION is empty at every declared coordinate; the clear-eyed INDIVIDUALS are not: at the slow-fear, devoted-attention corner (tau_safety 48, kappa 0.75), order ten agents across both seed sets entered on paths their own rollouts priced at their lives. Sacrifice with the price seen exists in this architecture as a rarity of particular agents in particular geometry, never as a property of a population. The persistent excess deaths elsewhere are trap-underestimation (the grip is not in the agents' physics): mistaken entries, not chosen ones. Phase 13 complete: FULL SIGHT (prospect_sees_grip, an Amendment 4 extension; what an agent knows is itself an axis). The answer, replicated over 768 runs: no wager cells and no up-front clear-eyed deaths anywhere, even at horizon 120 with the grip seen. Full sight collapses entry deaths (G2: mispricings, gone when priced). The replacement death: under full sight, pull-group deaths become ~100% starvation at the threshold (will not enter, will not leave), established as a mode; its excess over the seen-famine background (+3/+8 points) is suggestive, not settled. Phase 14 complete: THE OTHER IN THE LEDGER (Amendment 5: care prices the living target's peril; assistance physics lends a gripped partner speed). The wager still does not occur, replicated a third way: zero clear-eyed and up-front deaths in 216 runs; with care and working hands, the model produces assistance, not sacrifice. The replicated finding is the LIBERATION EFFECT: with any help, the trapped pool collapses 70-75 to 18-28 at onset+50 on both seed sets, because cohesive pairs are adjacent when caught: most rescue happens through proximity before it looks like a decision. The post-snapshot extraction gradient failed replication (small-n) and is withdrawn; the stranded-pairs protocol is the declared follow-up. Case register scaffolding, capability deltas, and the compiler protocol are committed under cases/ and docs/. Phase 15 complete: AUTHORITY AS TOPOLOGY. One-to-many bonds required zero mechanism (the attachment law never assumed mutuality): congregation emerges at ratio 4.7. L2 (single point of failure) failed as declared at a weak consistent 1.3x, and L3 (the additivity null for mass grief) was void for power: the plain-storm arena neither holds nor kills leaders enough to test either. The mire-leader arena is the declared follow-up. Mire-leader complete: M1 replicated (single point of failure, 2.9x/2.6x); M2 dissolved into the sharper finding: WHEN THE CENTER IS HELD, THE CIRCLE BURNS WITH IT (followers of caught leaders die 109/112 and 264/264 at kappa 2; no mourners remain, so mass grief is structurally unmeasurable there). Distant-death protocol run: D1 power FAILED on declared seeds (29 bereaved < 100; replication 171); D2 additivity null REFUTED both stages, sign replicated (crowd grief sub-additive: excess +0.172/+0.076 vs pair +0.327/+0.307), magnitude unsettled (thin leader arm, geometric selection); baseline starvation 0.000 everywhere: every starvation was a mourner's. Grief-geometry run: G0 manipulation check FAILED both stages (crowd mourners FARTHER from food, median 8.77/10.24 vs 8.43/7.96, yet starve less), so the place explanation is DEAD as pre-declared; G1 failed (tercile ratio 1.14/1.52 vs 2x bar; monotone gradient both stages, post hoc); G2 nominal pass not promoted (dead premise, 11.77 vs 11.80 bar, n 48). Sub-additivity of crowd grief stands unexplained. New candidate, speculative: contention trains hunger's attention weight and grief cannot shout it down. Attention-at-loss run: A0 split (0.414 vs 0.087 main, 0.069 vs 0.071 replication; bar was each stage, FAILED), A1 failed both stages with direction wrong at top (loud hunger starves MORE), A2 no shrinkage (22.1 vs 11.8 bar). Second mediator eliminated. Sub-additivity of crowd grief FILED OPEN after two pre-registered kills (place, trained voice); U-shape (middle tercile safest, 0.249/0.199) flagged post hoc, replicated in shape. Mediator hunt paused by decision. Remaining: stranded-pairs, matched-famine vigil, belief objects, transmission, inheritance, the retrodiction arc.
